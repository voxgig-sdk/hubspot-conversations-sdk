# HubspotConversations SDK

HubSpot Conversations API client, generated from the OpenAPI spec.

HubSpot Conversations API — merged from the vendor's per-API OpenAPI documents.

Learn more about HubSpot Conversations API at [hubapi.com](https://hubapi.com).

This is an unofficial SDK for the HubSpot Conversations public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/hubspot-conversations-sdk/](https://voxgig-sdk.github.io/hubspot-conversations-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **36 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new HubspotConversationsSDK()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = HubspotConversationsSDK.test({
  entity: {
    conversations_inbox_messages_public_thread: {
      test01: { id: 'test01' },
    },
  },
})
const conversationsinboxmessagespublicthread = await client.ConversationsInboxMessagesPublicThread().load({ id: 1 })
// conversationsinboxmessagespublicthread is the ConversationsInboxMessagesPublicThread entity, populated with mock data
// — call conversationsinboxmessagespublicthread.data() for the record itself
console.log(conversationsinboxmessagespublicthread)
```

### Python

```python
client = HubspotConversationsSDK.test()
conversationsinboxmessagespublicthread = client.ConversationsInboxMessagesPublicThread().load({"id": "test01"})
print(conversationsinboxmessagespublicthread)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = HubspotConversationsSDK::test([
    "entity" => ["conversationsinboxmessagespublicthread" => ["test01" => ["id" => "test01"]]],
]);
$conversationsinboxmessagespublicthread = $client->ConversationsInboxMessagesPublicThread()->load(["id" => "test01"]);
```

### Golang

```go
client := sdk.Test()
result, err := client.ConversationsInboxMessagesPublicThread(nil).Load(
    map[string]any{"id": "test01"}, nil,
)
```

### Lua

```lua
local client = sdk.test()
local result, err = client:ConversationsInboxMessagesPublicThread():load({ id = "test01" })
```

### JavaScript

```js
const client = HubspotConversationsSDK.test()
const conversationsinboxmessagespublicthread = await client.ConversationsInboxMessagesPublicThread().load({ id: 1 })
// conversationsinboxmessagespublicthread is the entity, populated with mock data
// — call conversationsinboxmessagespublicthread.data() for the record itself
console.log(conversationsinboxmessagespublicthread)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/hubspot-conversations` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-conversations-sdk/tags) |
| Python | `voxgig-sdk-hubspot-conversations` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-conversations-sdk/tags) |
| PHP | `voxgig-sdk/hubspot-conversations` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-conversations-sdk/tags) |
| Golang | `github.com/voxgig-sdk/hubspot-conversations-sdk/go` | `go get github.com/voxgig-sdk/hubspot-conversations-sdk/go@latest` |
| Lua | `voxgig-sdk-hubspot-conversations` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-conversations-sdk/tags) |
| JavaScript | `@voxgig-sdk/hubspot-conversations-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/hubspot-conversations-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/hubspot-conversations-sdk/go-cli` | `go install github.com/voxgig-sdk/hubspot-conversations-sdk/go-cli/cmd/hubspot-conversations@latest` |
| Go MCP server | `github.com/voxgig-sdk/hubspot-conversations-sdk/go-mcp` | `go get github.com/voxgig-sdk/hubspot-conversations-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { HubspotConversationsSDK } from '@voxgig-sdk/hubspot-conversations'

const client = new HubspotConversationsSDK({
  apikey: process.env.HUBSPOT_CONVERSATIONS_APIKEY,
})


// Load a specific conversationsinboxmessagespublicmessage (returns a ConversationsInboxMessagesPublicMessage)
const conversationsinboxmessagespublicmessage = await client.ConversationsInboxMessagesPublicMessage().load({
  thread_id: 1,
  id: 'example_id',
})
console.log(conversationsinboxmessagespublicmessage)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o hubspot-conversations-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "hubspot-conversations": {
      "command": "/abs/path/to/hubspot-conversations-mcp"
    }
  }
}
```

## Entities

The API exposes 36 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Channel** | The Channel entity (remove). | `/conversations/custom-channels/2026-09/{channelId}` |
| **ConversationsBatchResponsePublicActor** | The ConversationsBatchResponsePublicActor entity (create). | `/conversations/conversations/2026-09/actors/batch/read` |
| **ConversationsCollectionResponsePublicMessageForwardPaging** | The ConversationsCollectionResponsePublicMessageForwardPaging entity (list). | `/conversations/conversations/2026-09/threads/{threadId}/messages` |
| **ConversationsCollectionResponsePublicThreadForwardPaging** | The ConversationsCollectionResponsePublicThreadForwardPaging entity (list). | `/conversations/conversations/2026-09/threads` |
| **ConversationsCollectionResponseWithTotalPublicChannel** | The ConversationsCollectionResponseWithTotalPublicChannel entity (list). | `/conversations/conversations/2026-09/channels` |
| **ConversationsCollectionResponseWithTotalPublicChannelAccount** | The ConversationsCollectionResponseWithTotalPublicChannelAccount entity (list). | `/conversations/conversations/2026-09/channel-accounts` |
| **ConversationsCollectionResponseWithTotalPublicInbox** | The ConversationsCollectionResponseWithTotalPublicInbox entity (list). | `/conversations/conversations/2026-09/inboxes` |
| **ConversationsInboxMessagesBatchResponsePublicActor** | The ConversationsInboxMessagesBatchResponsePublicActor entity (create). | `/conversations/v3/conversations/actors/batch/read` |
| **ConversationsInboxMessagesCollectionResponsePublicMessage** | The ConversationsInboxMessagesCollectionResponsePublicMessage entity (list). | `/conversations/v3/conversations/threads/{threadId}/messages` |
| **ConversationsInboxMessagesCollectionResponsePublicThread** | The ConversationsInboxMessagesCollectionResponsePublicThread entity (list). | `/conversations/v3/conversations/threads` |
| **ConversationsInboxMessagesCollectionResponseWithTotalPublic** | The ConversationsInboxMessagesCollectionResponseWithTotalPublic entity (list). | `/conversations/v3/conversations/channel-accounts` |
| **ConversationsInboxMessagesCollectionResponseWithTotalPublic2** | The ConversationsInboxMessagesCollectionResponseWithTotalPublic2 entity (list). | `/conversations/v3/conversations/channels` |
| **ConversationsInboxMessagesCollectionResponseWithTotalPublic3** | The ConversationsInboxMessagesCollectionResponseWithTotalPublic3 entity (list). | `/conversations/v3/conversations/inboxes` |
| **ConversationsInboxMessagesPublicActor** | The ConversationsInboxMessagesPublicActor entity (load). | `/conversations/v3/conversations/actors/{actorId}` |
| **ConversationsInboxMessagesPublicChannel** | The ConversationsInboxMessagesPublicChannel entity (load). | `/conversations/v3/conversations/channels/{channelId}` |
| **ConversationsInboxMessagesPublicChannelAccount** | The ConversationsInboxMessagesPublicChannelAccount entity (load). | `/conversations/v3/conversations/channel-accounts/{channelAccountId}` |
| **ConversationsInboxMessagesPublicInbox** | The ConversationsInboxMessagesPublicInbox entity (load). | `/conversations/v3/conversations/inboxes/{inboxId}` |
| **ConversationsInboxMessagesPublicMessage** | The ConversationsInboxMessagesPublicMessage entity (create, load). | `/conversations/v3/conversations/threads/{threadId}/messages/{messageId}` |
| **ConversationsInboxMessagesPublicMessageContent** | The ConversationsInboxMessagesPublicMessageContent entity (load). | `/conversations/v3/conversations/threads/{threadId}/messages/{messageId}/original-content` |
| **ConversationsInboxMessagesPublicThread** | The ConversationsInboxMessagesPublicThread entity (load, update). | `/conversations/v3/conversations/threads/{threadId}` |
| **ConversationsPublicActor** | The ConversationsPublicActor entity (load). | `/conversations/conversations/2026-09/actors/{actorId}` |
| **ConversationsPublicChannel** | The ConversationsPublicChannel entity (load). | `/conversations/conversations/2026-09/channels/{channelId}` |
| **ConversationsPublicChannelAccount** | The ConversationsPublicChannelAccount entity (load). | `/conversations/conversations/2026-09/channel-accounts/{channelAccountId}` |
| **ConversationsPublicInbox** | The ConversationsPublicInbox entity (load). | `/conversations/conversations/2026-09/inboxes/{inboxId}` |
| **ConversationsPublicMessage** | The ConversationsPublicMessage entity (create, load). | `/conversations/conversations/2026-09/threads/{threadId}/messages/{messageId}` |
| **ConversationsPublicMessageContent** | The ConversationsPublicMessageContent entity (load). | `/conversations/conversations/2026-09/threads/{threadId}/messages/{messageId}/original-content` |
| **ConversationsPublicThread** | The ConversationsPublicThread entity (load, remove, update). | `/conversations/conversations/2026-09/threads/{threadId}` |
| **CustomChannelsCollectionResponseWithTotalPublicChannel** | The CustomChannelsCollectionResponseWithTotalPublicChannel entity (list). | `/conversations/custom-channels/2026-09` |
| **CustomChannelsCollectionResponseWithTotalPublicChannel2** | The CustomChannelsCollectionResponseWithTotalPublicChannel2 entity (list). | `/conversations/custom-channels/2026-09/{channelId}/channel-accounts` |
| **CustomChannelsPublicChannelAccount** | The CustomChannelsPublicChannelAccount entity (create, load, update). | `/conversations/custom-channels/2026-09/{channelId}/channel-accounts/{channelAccountId}` |
| **CustomChannelsPublicChannelAccountStagingToken** | The CustomChannelsPublicChannelAccountStagingToken entity (update). | `/conversations/custom-channels/2026-09/{channelId}/channel-account-staging-tokens/{accountToken}` |
| **CustomChannelsPublicChannelIntegrationChannel** | The CustomChannelsPublicChannelIntegrationChannel entity (create, load, update). | `/conversations/custom-channels/2026-09/{channelId}` |
| **CustomChannelsPublicConversationsMessage** | The CustomChannelsPublicConversationsMessage entity (create, load, update). | `/conversations/custom-channels/2026-09/{channelId}/messages/{messageId}` |
| **PublicThread** | The PublicThread entity (remove). | `/conversations/v3/conversations/threads/{threadId}` |
| **Thread** | The Thread entity (remove). | `/conversations/conversations/2026-09/threads/{threadId}` |
| **VisitorIdentificationIdentificationToken** | The VisitorIdentificationIdentificationToken entity (create). | `/visitor-identification/2026-09/tokens/create` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from hubspotconversations_sdk import HubspotConversationsSDK

client = HubspotConversationsSDK({
    "apikey": os.environ.get("HUBSPOT_CONVERSATIONS_APIKEY"),
})

```

### PHP

```php
<?php
require_once 'hubspotconversations_sdk.php';

$client = new HubspotConversationsSDK([
    "apikey" => getenv("HUBSPOT_CONVERSATIONS_APIKEY"),
]);

```

### Golang

```go
import sdk "github.com/voxgig-sdk/hubspot-conversations-sdk/go"

client := sdk.NewHubspotConversationsSDK(map[string]any{
    "apikey": os.Getenv("HUBSPOT_CONVERSATIONS_APIKEY"),
})


// Load a specific conversationsinboxmessagespublicmessage
conversationsInboxMessagesPublicMessage, err := client.ConversationsInboxMessagesPublicMessage(nil).Load(
    map[string]any{"thread_id": 1, "id": "example_id"}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(conversationsInboxMessagesPublicMessage)
```

### Lua

```lua
local sdk = require("hubspot-conversations_sdk")

local client = sdk.new({
  apikey = os.getenv("HUBSPOT_CONVERSATIONS_APIKEY"),
})

```

### JavaScript

```js
const { HubspotConversationsSDK } = require('@voxgig-sdk/hubspot-conversations-js')

const client = new HubspotConversationsSDK({
  apikey: process.env.HUBSPOT_CONVERSATIONS_APIKEY,
})


// Load a specific conversationsinboxmessagespublicmessage (returns the entity)
const conversationsinboxmessagespublicmessage = await client.ConversationsInboxMessagesPublicMessage().load({
  thread_id: 1,
  id: 'example_id',
})
console.log(conversationsinboxmessagespublicmessage)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.hubapi.com](https://api.hubapi.com)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the HubSpot Conversations API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
